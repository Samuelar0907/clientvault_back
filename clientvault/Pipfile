[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
uvicorn = "*"
apscheduler = "*"
pymysql = "*"
sqlalchemy = "*"
fastapi = "*"
psycopg2-binary = "*"
alembic = "*"

[dev-packages]

[requires]
python_version = "3.12"

[scripts]
start = "uvicorn src.server:app --reload --host 0.0.0.0 --port ${APP_PORT}"
make-migrations = "alembic revision --autogenerate -m 'migrations'"
migrate = "alembic upgrade head"